2. 通过 Python 扩展查看

2.1 安装 Python 扩展

若尚未安装，可在 VSCode 扩展商店搜索并安装 Python 扩展（由 Microsoft 提供）。
2.2 查看环境和包

打开 Python 文件（.py）。
点击底部状态栏中的 Python 环境版本（如 Python 3.10.0）。
在弹出的环境选择面板中，点击 ... 并选择 "查看所有 Python 环境"。
在左侧列表中选择环境，右侧会显示该环境下的已安装包。
